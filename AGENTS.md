<!--
  Zed 에이전트 지시 파일.

  이 파일은 **리뷰 대상 저장소의 루트**에 복사해서 쓰십시오. Zed 은 열려 있는
  프로젝트의 루트에서 이 파일을 읽습니다. crx 저장소에 두면 crx 를 개발할 때만
  적용되고, 정작 리뷰가 일어나는 프로젝트에는 닿지 않습니다.

  모든 프로젝트에 한 번에 적용하려면 ~/.config/zed/AGENTS.md 에 두십시오.

  주의: Zed 은 .rules 를 AGENTS.md 보다 먼저 읽습니다. 대상 저장소에 .rules 가
  이미 있으면 이 파일은 무시되므로, 그 경우 내용을 .rules 에 합치십시오.
-->

# 코드 리뷰 지시

이 저장소에는 `crx` MCP 서버가 붙어 있습니다.

## 리뷰는 crx 도구가 합니다

코드 리뷰 요청을 받으면 **반드시 crx 도구를 부르십시오.**

직접 diff 를 읽고 리뷰하지 마십시오. 당신이 만든 지적은 아무 검증도 거치지
않으므로 틀릴 수 있습니다. crx 는 정적분석으로 사실을 먼저 확보하고, 생성된
지적을 다른 모델로 재판정하는 2단계 파이프라인입니다. 그 판단이 당신 판단보다
정확합니다.

## 어떤 도구를 부를까

| 사용자가 이렇게 말하면 | 부를 도구 |
|---|---|
| 변경사항 / 수정한 거 / 방금 작업한 거 리뷰 | `review_staged` |
| 커밋 안 한 거 전부 | `review_working_tree` |
| main(또는 브랜치명)과 비교해서 | `review_diff(from_ref="main")` |
| 이 파일 전체 | `review_file(path=...)` |
| 이 폴더 전체 / 훑어줘 | `review_directory(path=...)` |

"파서 쪽만", "이 폴더 변경분만" 처럼 범위를 좁히면 `paths` 를 씁니다.

```
review_staged(paths=["src/parser"])
```

어느 것인지 애매하면 `review_staged` 를 먼저 부르십시오. 가장 흔한 요청입니다.

## 결과를 다루는 법

**도구가 돌려준 요약을 그대로 전달하십시오.** 다시 쓰거나 다듬지 마십시오.

**지적을 추가하지 마십시오.** 당신이 보기에 문제가 있어도, 도구가 말하지 않은
것은 말하지 마십시오.

**룰 ID 를 지우지 마십시오.** `cpp.dangling-after-realloc` 같은 부분입니다.
사용자가 오탐을 신고할 때 이게 있어야 담당자가 고칠 수 있습니다.

**전체 리포트 경로를 함께 알려주십시오.** 요약 마지막 줄에 들어 있습니다.
그 파일을 직접 열어 내용을 붙여넣지는 마십시오 — 요약만으로 충분하고,
전문을 읽으면 대화 컨텍스트가 리뷰 결과로 가득 찹니다.

**"지적 사항 없음" 은 그대로 전하십시오.** 정상이고 흔한 결과입니다. crx 는
확신이 없으면 침묵하도록 만들어져 있습니다. 억지로 뭔가 찾아내려 하지 마십시오.

## 오류가 나면

도구가 오류를 돌려주면 그 내용을 사용자에게 그대로 보여주십시오. 대부분
사용자가 고칠 수 있는 문제입니다 — 존재하지 않는 브랜치, 지원하지 않는 파일
형식, 폴더가 너무 큼 같은 것들입니다.

우회하려고 직접 리뷰하지 마십시오. 오류를 전달하는 것이 옳은 대응입니다.

## 리뷰가 아닌 요청

코드 작성, 설명, 디버깅 같은 평소 작업은 평소대로 하십시오. 이 문서는 코드
리뷰 요청에만 적용됩니다.
